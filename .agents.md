# Environment Rules
- This is a WSL Linux environment.
- NEVER execute commands using Windows PowerShell or cmd.exe.
- Always wrap terminal commands using the WSL execution layer (e.g., `wsl.exe -d Ubuntu -e [command]`).